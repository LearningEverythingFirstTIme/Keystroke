<Window x:Class="KeystrokeApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:KeystrokeApp.Controls"
        Title="Keystroke Settings"
        Height="920"
        Width="1440"
        MinHeight="780"
        MinWidth="1180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#0B1220">

    <Window.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="PrivacyTint" Color="#3B2A20"/>
            <SolidColorBrush x:Key="PrivacyBorder" Color="#6A4C3B"/>
            <SolidColorBrush x:Key="LearningTint" Color="#182E2B"/>
            <SolidColorBrush x:Key="LearningBorder" Color="#245D50"/>
            <SolidColorBrush x:Key="ExperimentalTint" Color="#33250F"/>
            <SolidColorBrush x:Key="ExperimentalBorder" Color="#7A5922"/>

            <Style TargetType="Border" x:Key="PrivacyCardStyle" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Background" Value="{StaticResource PrivacyTint}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrivacyBorder}"/>
            </Style>

            <Style TargetType="Border" x:Key="LearningCardStyle" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Background" Value="{StaticResource LearningTint}"/>
                <Setter Property="BorderBrush" Value="{StaticResource LearningBorder}"/>
            </Style>

            <Style TargetType="Border" x:Key="ExperimentalCardStyle" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Background" Value="{StaticResource ExperimentalTint}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ExperimentalBorder}"/>
            </Style>

            <Style x:Key="PillStyle" TargetType="Border">
                <Setter Property="Background" Value="#18263E"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderStrong}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="3"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Margin" Value="0,0,8,8"/>
            </Style>

            <Style x:Key="NavRadioStyle" TargetType="RadioButton">
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="NavBorder"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusControl}"
                                    Padding="14,12">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="NavBorder" Property="Background" Value="#172338"/>
                                    <Setter TargetName="NavBorder" Property="BorderBrush" Value="{StaticResource BorderStrong}"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="NavBorder" Property="Background" Value="#142238"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SwitchCheckBoxStyle" TargetType="CheckBox">
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid Width="52" Height="30">
                                <Border x:Name="Track"
                                        Background="#415A77"
                                        CornerRadius="15"
                                        BorderBrush="#5A7696"
                                        BorderThickness="1"/>
                                <Ellipse x:Name="Thumb"
                                         Width="22"
                                         Height="22"
                                         Fill="White"
                                         Margin="4,0,0,0"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{StaticResource Accent}"/>
                                    <Setter TargetName="Track" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Track" Property="Opacity" Value="0.45"/>
                                    <Setter TargetName="Thumb" Property="Opacity" Value="0.55"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FeatureCardStyle" TargetType="Border" BasedOn="{StaticResource CardStyle}">
                <Setter Property="Padding" Value="18"/>
            </Style>

            <Style x:Key="InsetTileStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource BgInput}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource RadiusCard}"/>
                <Setter Property="Padding" Value="14"/>
            </Style>

            <Style x:Key="InsetPanelStyle" TargetType="Border" BasedOn="{StaticResource InsetTileStyle}">
                <Setter Property="Padding" Value="16"/>
            </Style>

            <Style x:Key="ThemeCardButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <Style x:Key="PresetButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                <Setter Property="Background" Value="#16263C"/>
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
                <Setter Property="Padding" Value="14,10"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource BgWindow}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource BgSidebar}"
                BorderBrush="{StaticResource Border}"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="18">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="Keystroke"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="24"
                               FontWeight="Bold"/>
                    <TextBlock Text="Settings"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="13"
                               Margin="0,4,0,24"/>

                    <RadioButton x:Name="NavOverviewButton" GroupName="SettingsNav" IsChecked="True" Checked="NavigateSection_Checked" Tag="Overview" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Overview"/>
                            <TextBlock Text="Quick read on how Keystroke feels right now" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavSuggestionsButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="Suggestions" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Suggestions"/>
                            <TextBlock Text="Tune timing, length, and creativity" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavPersonalizationButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="Personalization" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Your AI Profile"/>
                            <TextBlock Text="Profile building, personalization, and privacy" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavAnalyticsButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="Analytics" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Analytics"/>
                            <TextBlock Text="Writing trends, progress, and insights" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavAppControlButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="AppControl" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="App Control"/>
                            <TextBlock Text="Choose exactly where Keystroke appears" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavAppearanceButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="Appearance" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Appearance"/>
                            <TextBlock Text="Theme and preview styling" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="NavAdvancedButton" GroupName="SettingsNav" Checked="NavigateSection_Checked" Tag="Advanced" Style="{StaticResource NavRadioStyle}">
                        <StackPanel>
                            <TextBlock Text="Advanced"/>
                            <TextBlock Text="Engine setup, providers, and prompt" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="24,22,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer x:Name="MainSectionScrollViewer"
                          Grid.Row="0"
                          VerticalScrollBarVisibility="Visible"
                          HorizontalScrollBarVisibility="Disabled"
                          CanContentScroll="False"
                          ClipToBounds="True"
                          Padding="0,0,6,0">
                <StackPanel>
                <Grid x:Name="MainTabControl" ClipToBounds="True">
                <StackPanel x:Name="OverviewSection" Visibility="Visible">
                        <TextBlock Text="Overview" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="Start here for the simplest read on your current setup." Style="{StaticResource SectionIntroStyle}"/>

                        <Border Style="{StaticResource CardStyle}" Margin="0,20,0,18">
                            <StackPanel>
                                <TextBlock x:Name="HeroTitleText" Text="Keystroke is tuned for balanced suggestions" Foreground="{StaticResource TextPrimary}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock x:Name="HeroSubtitleText" Text="Paced for flow, with 1 option ready when you pause." Foreground="{StaticResource TextSecondary}" FontSize="13" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                <WrapPanel Margin="0,14,0,0">
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="HeroEngineStatusText" Text="Connected" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="HeroLearningStatusText" Text="Learning off" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="HeroContextStatusText" Text="Context on" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="HeroAppControlStatusText" Text="App control: open" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="HeroThemeStatusText" Text="Theme: Midnight" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                </WrapPanel>
                                <WrapPanel Margin="0,14,0,0">
                                    <Button x:Name="PresetMinimalBtn" Content="Minimal" Style="{StaticResource PresetButton}" Margin="0,0,10,0" Click="PresetMinimal_Click"/>
                                    <Button x:Name="PresetBalancedBtn" Content="Balanced" Style="{StaticResource PresetButton}" Margin="0,0,10,0" Click="PresetBalanced_Click"/>
                                    <Button x:Name="PresetMaxBtn" Content="Maximum" Style="{StaticResource PresetButton}" Click="PresetMaximum_Click"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="330"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,18,0">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                    <StackPanel>
                                        <TextBlock Text="Current status" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <Grid Margin="0,16,0,0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="180"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="Engine" Foreground="{StaticResource TextMuted}" Margin="0,0,0,10"/>
                                            <TextBlock x:Name="OverviewEngineStatusText" Grid.Column="1" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                                            <TextBlock Grid.Row="1" Text="AI Profile" Foreground="{StaticResource TextMuted}" Margin="0,0,0,10"/>
                                            <TextBlock x:Name="OverviewLearningStatusText" Grid.Row="1" Grid.Column="1" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                                            <TextBlock Grid.Row="2" Text="Privacy" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock x:Name="OverviewPrivacyStatusText" Grid.Row="2" Grid.Column="1" Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                    <StackPanel>
                                        <TextBlock Text="Privacy at a glance" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="OverviewPrivacyDetailText" Text="Screen reading is on. Learning is off." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <Border x:Name="PersonalizationTeaserCard" Style="{StaticResource SoftCardStyle}" Margin="0,0,0,18">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="AI Profile" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                                <TextBlock x:Name="OverviewProfileSummaryText" Text="Keystroke has not started building your profile yet." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                                <TextBlock x:Name="OverviewProfileDetailText" Text="Open Your AI Profile to see what Keystroke is collecting and whether it is being used." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <Button x:Name="PersonalizationTeaserButton" Grid.Column="1" Content="See Your Profile →" Style="{StaticResource SecondaryButton}" Margin="18,0,0,0" VerticalAlignment="Top" Click="PersonalizationTeaser_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource SoftCardStyle}">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="App Control" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                                <TextBlock x:Name="OverviewAppControlSummaryText" Text="Keystroke is available everywhere except blocked apps." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                                <TextBlock x:Name="OverviewAppControlDetailText" Text="Open App Control to decide where the overlay should stay quiet." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <Button Grid.Column="1" Content="Open App Control" Style="{StaticResource SecondaryButton}" Margin="18,0,0,0" VerticalAlignment="Top" Click="OpenAppControl_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource SoftCardStyle}" Margin="0,18,0,0">
                                    <StackPanel>
                                        <TextBlock Text="What changes most often" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Text="Use Suggestions when you want Keystroke faster, shorter, or more cautious. Use Your AI Profile when you want to understand what Keystroke is learning and whether it is actively personalizing. Use App Control when you want the overlay to stay out of specific apps."
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="12"
                                                   Margin="0,8,0,0"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                </StackPanel>

                <StackPanel x:Name="SuggestionsSection" Visibility="Collapsed">
                        <TextBlock Text="Suggestions" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="Tune when suggestions appear, how long they are, and how adventurous they feel." Style="{StaticResource SectionIntroStyle}"/>

                        <Border x:Name="PreviewBorder" Background="{StaticResource BgPreview}" BorderBrush="{StaticResource BorderStrong}" BorderThickness="1" CornerRadius="4" Padding="18" Margin="0,20,0,18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Margin="0,0,14,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <Ellipse x:Name="PreviewAccentDot" Width="10" Height="10" Fill="{StaticResource Accent}" VerticalAlignment="Center"/>
                                        <TextBlock Text="Live Preview" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <Border x:Name="PreviewSuggestionBubble" Background="#101825" CornerRadius="{StaticResource RadiusCard}" Padding="16,12" BorderBrush="#4A5B7390" BorderThickness="1">
                                        <StackPanel>
                                            <TextBlock x:Name="PreviewSuggestionText" Text=", and I'll send the cleaned-up draft over later this afternoon." FontFamily="Cascadia Code, Consolas" FontSize="14" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <TextBlock Text="Tab accept &#x00b7; Ctrl+&#x2192; word &#x00b7; Esc dismiss" FontFamily="Segoe UI" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <TextBlock x:Name="PreviewMetaText" Text="Standard (8-15 words) | 1 suggestion | Balanced creativity" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0"/>
                                    <TextBlock x:Name="PreviewLengthDetailText" Text="Expected completion size appears here." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                    <TextBlock Text="Response Timing" Foreground="{StaticResource TextPrimary}" FontSize="13" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="PreviewDebounceStatusText" Text="Suggestions appear after you pause." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>

                                    <Grid Margin="0,14,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="After punctuation" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                                        <ProgressBar x:Name="PreviewDebounceProgressBar" Grid.Column="1" Minimum="0" Maximum="100" Height="8" VerticalAlignment="Center" Value="0"/>
                                        <TextBlock x:Name="PreviewDebounceProgressText" Grid.Column="2" Text="300ms" Foreground="{StaticResource Accent}" FontSize="11" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </Grid>

                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="While typing" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                                        <ProgressBar x:Name="PreviewFastDebounceProgressBar" Grid.Column="1" Minimum="0" Maximum="100" Height="8" VerticalAlignment="Center" Value="0"/>
                                        <TextBlock x:Name="PreviewFastDebounceProgressText" Grid.Column="2" Text="100ms" Foreground="{StaticResource Accent}" FontSize="11" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Suggestion length" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="Choose how much Keystroke should finish for you in one go." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>
                                <ComboBox x:Name="LengthCombo" Margin="0,16,0,0" SelectionChanged="Setting_Changed">
                                    <ComboBoxItem Content="Brief (3-5 words)" Tag="brief"/>
                                    <ComboBoxItem Content="Standard (8-15 words)" Tag="standard"/>
                                    <ComboBoxItem Content="Extended (15-30 words)" Tag="extended"/>
                                    <ComboBoxItem Content="Unlimited (30-50 words)" Tag="unlimited"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Responsiveness" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="These controls shape when Keystroke feels ready to step in. Watch the Live Preview above to feel the timing change in real time." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>

                                <Grid Margin="0,18,0,4">
                                    <TextBlock Text="Start predicting after" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="MinCharsLabel" Text="3" HorizontalAlignment="Right" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                </Grid>
                                <Slider x:Name="MinCharsSlider" Minimum="1" Maximum="10" Value="3" IsSnapToTickEnabled="True" TickFrequency="1" ValueChanged="MinCharsSlider_Changed"/>
                                <TextBlock Text="Higher values make Keystroke wait for more context before it steps in." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>

                                <Border Background="{StaticResource Border}" Height="1" Margin="0,16,0,16"/>

                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Timing after punctuation" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="DebounceLabel" Text="300ms" HorizontalAlignment="Right" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                </Grid>
                                <Slider x:Name="DebounceSlider" Minimum="100" Maximum="1500" Value="300" ValueChanged="DebounceSlider_Changed"/>
                                <TextBlock Text="How long Keystroke waits after a clear pause like punctuation or a sentence break." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>

                                <Grid Margin="0,16,0,4">
                                    <TextBlock Text="Timing while typing" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBlock x:Name="FastDebounceLabel" Text="100ms" HorizontalAlignment="Right" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                </Grid>
                                <Slider x:Name="FastDebounceSlider" Minimum="50" Maximum="500" Value="100" ValueChanged="FastDebounceSlider_Changed"/>
                                <TextBlock Text="How quickly Keystroke reacts when you briefly pause in the middle of a thought." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="How many options to show" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <Grid Margin="0,16,0,4">
                                    <TextBlock Text="1 = fastest" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                    <TextBlock x:Name="SuggestionsLabel" Text="3" HorizontalAlignment="Center" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                    <TextBlock Text="5 = most options" HorizontalAlignment="Right" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                </Grid>
                                <Slider x:Name="SuggestionsSlider" Minimum="1" Maximum="5" Value="3" IsSnapToTickEnabled="True" TickFrequency="1" ValueChanged="SuggestionsSlider_Changed"/>
                                <TextBlock Text="More options help when you like cycling choices. Fewer options keep latency and cost down." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="Creativity" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="Lower values stay safer. Higher values open up more expressive guesses." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>
                                <Grid Margin="0,16,0,4">
                                    <TextBlock Text="Conservative" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                    <TextBlock x:Name="TempLabel" Text="0.3" HorizontalAlignment="Center" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                    <TextBlock Text="Creative" HorizontalAlignment="Right" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                </Grid>
                                <Slider x:Name="TempSlider" Minimum="0" Maximum="1.0" Value="0.3" TickFrequency="0.1" ValueChanged="TempSlider_Changed"/>
                            </StackPanel>
                        </Border>
                </StackPanel>

                <StackPanel x:Name="PersonalizationSection" Visibility="Collapsed">
                        <TextBlock Text="Your AI Profile" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="See what Keystroke is learning quietly in the background, and choose whether that profile should actively shape completions." Style="{StaticResource SectionIntroStyle}"/>

                        <Border x:Name="LearningUpgradeBanner" Style="{StaticResource LearningCardStyle}" Margin="0,20,0,18" Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Personalized AI is ready when you are" Foreground="{StaticResource TextPrimary}" FontSize="17" FontWeight="SemiBold"/>
                                    <TextBlock Text="Keystroke is already tracking your accepted completions locally. Enter a license key to turn that passive profile into live prompt guidance." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel Margin="0,14,0,0">
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Matches your phrasing" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Builds writing style hints" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Uses your voice" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    </WrapPanel>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Unlock with License Key →" Style="{StaticResource PrimaryButton}" Margin="18,0,0,0" VerticalAlignment="Top" Click="ActivateLearning_Click"/>
                            </Grid>
                        </Border>

                        <Border x:Name="ProfileStatusCard" Style="{StaticResource LearningCardStyle}" Margin="0,20,0,18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock x:Name="ProfileStatusTitleText" Text="Your AI profile has not started yet." Foreground="{StaticResource TextPrimary}" FontSize="17" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="ProfileStatusBodyText" Text="Accept a few suggestions and Keystroke will start building a profile you can keep passive or turn on for active personalization." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel Margin="0,14,0,0">
                                        <Border Style="{StaticResource PillStyle}">
                                            <TextBlock x:Name="ProfileSignalsBadgeText" Text="0 signals" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                        </Border>
                                        <Border Style="{StaticResource PillStyle}">
                                            <TextBlock x:Name="ProfileContextsBadgeText" Text="No contexts yet" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                        </Border>
                                        <Border Style="{StaticResource PillStyle}">
                                            <TextBlock x:Name="ProfileModeBadgeText" Text="Passive capture only" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                        </Border>
                                    </WrapPanel>
                                </StackPanel>
                                <Button x:Name="ProfileStatusActionButton" Grid.Column="1" Content="Unlock with License Key" Style="{StaticResource SecondaryButton}" Margin="18,0,0,0" VerticalAlignment="Top" Click="ActivateLearning_Click"/>
                            </Grid>
                        </Border>

                        <Grid Margin="0,20,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource FeatureCardStyle}" Margin="0,0,12,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Remember recent text" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="RollingContextStatusText" Text="Keeps continuity across the latest part of what you were writing." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <CheckBox x:Name="RollingContextCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBoxStyle}" Checked="Setting_CheckChanged" Unchecked="Setting_CheckChanged" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource PrivacyCardStyle}" Margin="12,0,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Use screen context" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="OcrStatusText" Text="Reads visible text on screen so suggestions stay grounded in what you are looking at." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <CheckBox x:Name="OcrEnabledCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBoxStyle}" Checked="Setting_CheckChanged" Unchecked="Setting_CheckChanged" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" x:Name="LearningFeatureCard" Style="{StaticResource LearningCardStyle}" Margin="0,0,12,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Personalized AI" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="LearningFeatureStatusText" Text="Keystroke can use your accepted completions to better match your writing." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <CheckBox x:Name="LearningEnabledCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBoxStyle}" Checked="Setting_CheckChanged" Unchecked="Setting_CheckChanged" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Border Grid.Column="1" x:Name="StyleProfileCard" Style="{StaticResource CardStyle}" Margin="12,0,0,0">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Writing style" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="StyleProfileFeatureText" Text="Builds a writing profile over time so phrasing and tone feel more like you." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <CheckBox x:Name="StyleProfileCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBoxStyle}" Checked="StyleProfileCheck_Changed" Unchecked="StyleProfileCheck_Changed" VerticalAlignment="Center"/>
                                    </Grid>

                                    <Grid Margin="0,16,0,4">
                                        <TextBlock Text="Update after this many accepts" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock x:Name="StyleProfileIntervalLabel" Text="30" HorizontalAlignment="Right" Foreground="{StaticResource Accent}" FontWeight="SemiBold"/>
                                    </Grid>
                                    <Slider x:Name="StyleProfileIntervalSlider" Minimum="10" Maximum="100" Value="30" TickFrequency="5" IsSnapToTickEnabled="True" ValueChanged="StyleProfileIntervalSlider_Changed"/>
                                    <TextBlock x:Name="StyleProfileDependencyText" Text="Needs Personalized AI to be on." Foreground="{StaticResource Warning}" FontSize="11" Margin="0,10,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="App-aware tone" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                <TextBlock Text="Keystroke always adapts across chat, email, code, browser, terminal, and document contexts." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                <Border Style="{StaticResource PillStyle}" Margin="0,14,0,0" HorizontalAlignment="Left">
                                    <TextBlock Text="Always on" Foreground="{StaticResource Success}" FontSize="11" FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource PrivacyCardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Privacy / Data Flow" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="This preview shows the shape of the prompt Keystroke would send right now. Sensitive active text stays redacted here even when the runtime predictor is allowed to continue it." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>

                                <WrapPanel Margin="0,14,0,0">
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="PromptProviderStatusText" Text="Provider" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="PromptAppStatusText" Text="App status" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    <Border Style="{StaticResource PillStyle}"><TextBlock x:Name="PromptFilteringStatusText" Text="App filter" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                </WrapPanel>

                                <TextBlock x:Name="PromptAppReasonText" Text="App availability details appear here." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                <TextBlock x:Name="PromptTypedStatusText" Text="Typed input status appears here." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                <TextBlock x:Name="PromptWillSendText" Text="Prediction send status appears here." Foreground="{StaticResource TextPrimary}" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>

                                <Grid Margin="0,16,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Style="{StaticResource InsetTileStyle}" Margin="0,0,12,12">
                                        <StackPanel>
                                            <TextBlock Text="Typed prefix (redacted preview)" Foreground="{StaticResource TextPrimary}" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="PromptTypedPreviewText" Text="Type in another app to populate this." Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Style="{StaticResource InsetTileStyle}" Margin="12,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="Profile hints" Foreground="{StaticResource TextPrimary}" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="PromptLearningStatusText" Text="Learning hint status appears here." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                            <TextBlock x:Name="PromptLearningPreviewText" Text="Learning hints preview appears here." Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="0" Grid.Row="1" Style="{StaticResource InsetTileStyle}" Margin="0,0,12,0">
                                        <StackPanel>
                                            <TextBlock Text="Screen context" Foreground="{StaticResource TextPrimary}" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="PromptScreenPreviewText" Text="Screen context preview appears here." Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource InsetTileStyle}" Margin="12,0,0,0">
                                        <StackPanel>
                                            <TextBlock Text="Recently written context" Foreground="{StaticResource TextPrimary}" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock x:Name="PromptRollingPreviewText" Text="Rolling context preview appears here." Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <TextBlock Text="Prompt sections sent upstream" Foreground="{StaticResource TextPrimary}" FontSize="13" FontWeight="SemiBold" Margin="0,18,0,8"/>
                                <TextBox x:Name="PromptPreviewText" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="220" FontFamily="Consolas" FontSize="11"/>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="330"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,18,0">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                    <StackPanel>
                                        <TextBlock Text="Your AI profile" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <StackPanel x:Name="ProfileIntelligencePanel">
                                            <Grid Margin="0,16,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Style="{StaticResource InsetTileStyle}" Margin="0,0,8,0">
                                                    <StackPanel>
                                                        <TextBlock x:Name="StatsTotalAccepted" Text="0" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Accent}"/>
                                                        <TextBlock Text="Accepted signals" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                    </StackPanel>
                                                </Border>
                                                <Border Grid.Column="1" Style="{StaticResource InsetTileStyle}" Margin="4,0">
                                                    <StackPanel>
                                                        <TextBlock x:Name="StatsAcceptRate" Text="-" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Success}"/>
                                                        <TextBlock Text="Accept rate" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                    </StackPanel>
                                                </Border>
                                                <Border Grid.Column="2" Style="{StaticResource InsetTileStyle}" Margin="8,0,0,0">
                                                    <StackPanel>
                                                        <TextBlock x:Name="StatsLastActivity" Text="Never" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,5,0,0"/>
                                                        <TextBlock Text="Last activity" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                    </StackPanel>
                                                </Border>
                                            </Grid>
                                            <TextBlock Text="By category" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,18,0,8"/>
                                            <StackPanel x:Name="CategoryBreakdownPanel"/>
                                            <TextBlock Text="Top contexts" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,18,0,8"/>
                                            <StackPanel x:Name="ContextBreakdownPanel"/>
                                            <TextBlock Text="Adaptive tuning" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,18,0,8"/>
                                            <StackPanel x:Name="AdaptiveTuningPanel"/>
                                        </StackPanel>
                                        <StackPanel x:Name="CategoryBreakdownLocked" Visibility="Collapsed" Margin="0,16,0,0">
                                            <Border Style="{StaticResource InsetTileStyle}">
                                                <StackPanel>
                                                    <TextBlock x:Name="CategoryBreakdownLockedTitleText" Text="Profile intelligence is locked for free users." Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="SemiBold"/>
                                                    <TextBlock x:Name="CategoryBreakdownLockedBodyText" Text="0 completions tracked — enter a license key to unlock category intelligence, writing style, and voice guidance." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource CardStyle}">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="Profile data" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                            <Button Grid.Column="1" Content="Reset AI profile" Style="{StaticResource SecondaryButton}" Click="ResetLearning_Click"/>
                                        </Grid>
                                        <TextBlock Text="Clear saved profile signals and rebuild from scratch if Keystroke starts feeling out of sync with how you write." Foreground="{StaticResource TextMuted}" FontSize="12" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <Border Style="{StaticResource LearningCardStyle}" Margin="0,0,0,18">
                                    <StackPanel>
                                        <TextBlock Text="Writing style" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="StyleProfileStatus" Text="" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        <Grid x:Name="StyleProgressPanel" Margin="0,16,0,12" Visibility="Collapsed">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid Width="88" Height="88">
                                                <Ellipse Width="88" Height="88" Stroke="#29405D" StrokeThickness="7" Fill="Transparent"/>
                                                <Ellipse x:Name="ProgressArc" Width="88" Height="88" Stroke="{StaticResource Accent}" StrokeThickness="7" Fill="Transparent" StrokeDashArray="0 38.746" RenderTransformOrigin="0.5,0.5">
                                                    <Ellipse.RenderTransform>
                                                        <RotateTransform Angle="-90"/>
                                                    </Ellipse.RenderTransform>
                                                </Ellipse>
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock x:Name="ProgressCount" Text="0/30" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"/>
                                                    <TextBlock x:Name="ProgressLabel" Text="accepts" FontSize="9" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0,0,0">
                                                <TextBlock x:Name="ProgressMessage" Text="until next profile update" Foreground="{StaticResource TextSecondary}" FontSize="12" TextWrapping="Wrap"/>
                                                <TextBlock x:Name="ProgressGenerated" Text="" Foreground="{StaticResource Success}" FontSize="11" Margin="0,6,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                        <WrapPanel x:Name="CategoryBadgesPanel"/>
                                    </StackPanel>
                                </Border>

                                <Border Style="{StaticResource SoftCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Your voice" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="VocabProfileStatus" Text="No fingerprint generated yet." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,10" TextWrapping="Wrap"/>
                                        <WrapPanel x:Name="VocabCategoryBadgesPanel"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                </StackPanel>

                <StackPanel x:Name="AnalyticsSection" Visibility="Collapsed">
                        <TextBlock Text="Analytics" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="See how your writing profile is evolving — acceptance rates, quality trends, and context insights." Style="{StaticResource SectionIntroStyle}"/>

                        <!-- Locked teaser for free users -->
                        <Border x:Name="AnalyticsLockedCard" Style="{StaticResource LearningCardStyle}" Margin="0,20,0,18" Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Writing analytics are ready when you are" Foreground="{StaticResource TextPrimary}" FontSize="17" FontWeight="SemiBold"/>
                                    <TextBlock Text="Keystroke is already tracking your writing patterns. Enter a license key to unlock trends, progress milestones, and context insights." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel Margin="0,14,0,0">
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Score trajectories" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Weekly comparisons" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                        <Border Style="{StaticResource PillStyle}"><TextBlock Text="Milestones" Foreground="{StaticResource TextSecondary}" FontSize="11"/></Border>
                                    </WrapPanel>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Unlock with License Key" Style="{StaticResource SecondaryButton}" Margin="18,0,0,0" VerticalAlignment="Top" Click="ActivateLearning_Click"/>
                            </Grid>
                        </Border>

                        <!-- Pro analytics content -->
                        <StackPanel x:Name="AnalyticsProContent" Visibility="Visible">

                            <!-- Hero Summary Card -->
                            <Border x:Name="AnalyticsHeroCard" Style="{StaticResource LearningCardStyle}" Margin="0,20,0,18">
                                <StackPanel>
                                    <TextBlock x:Name="AnalyticsHeroTitle" Text="Your writing profile is just getting started" Foreground="{StaticResource TextPrimary}" FontSize="22" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock x:Name="AnalyticsHeroSubtitle" Text="Start accepting suggestions and your analytics will appear here." Foreground="{StaticResource TextSecondary}" FontSize="13" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel x:Name="AnalyticsHeroPills" Margin="0,14,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Week-over-Week Comparison Row -->
                            <Grid x:Name="AnalyticsWeekCompareGrid" Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource InsetTileStyle}" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="Acceptance rate" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                        <TextBlock x:Name="AnalyticsWeekAcceptRate" Text="--" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Success}"/>
                                        <TextBlock x:Name="AnalyticsWeekAcceptDelta" Text="" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource InsetTileStyle}" Margin="4,0">
                                    <StackPanel>
                                        <TextBlock Text="Words assisted" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                        <TextBlock x:Name="AnalyticsWeekWordsAssisted" Text="--" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Accent}"/>
                                        <TextBlock x:Name="AnalyticsWeekWordsDelta" Text="" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource InsetTileStyle}" Margin="8,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="Avg quality" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                        <TextBlock x:Name="AnalyticsWeekAvgQuality" Text="--" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                        <TextBlock x:Name="AnalyticsWeekQualityDelta" Text="" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Score Trajectory Card -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                <StackPanel>
                                    <TextBlock Text="Intelligence score over time" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <WrapPanel x:Name="AnalyticsScoreCategoryPills" Margin="0,10,0,8"/>
                                    <Border Background="#21262D" CornerRadius="6" Padding="2" Margin="0,4,0,0">
                                        <controls:SparklineControl x:Name="AnalyticsScoreSparkline" Height="140"/>
                                    </Border>
                                    <TextBlock x:Name="AnalyticsScoreEmptyText" Text="Score history will appear after your first profile recompute." Foreground="{StaticResource TextMuted}" FontSize="12" FontStyle="Italic" Margin="0,8,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Daily Activity Card -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                <StackPanel>
                                    <TextBlock Text="Daily activity \u2014 last 30 days" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <Border Background="#3FB950" Width="10" Height="10" CornerRadius="2" Margin="0,0,4,0"/>
                                        <TextBlock Text="Accepted" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,0,12,0"/>
                                        <Border Background="#2EA98F" Width="10" Height="10" CornerRadius="2" Margin="0,0,4,0"/>
                                        <TextBlock Text="Native" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,0,12,0"/>
                                        <Border Background="#484F58" Width="10" Height="10" CornerRadius="2" Margin="0,0,4,0"/>
                                        <TextBlock Text="Dismissed" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                    </StackPanel>
                                    <Border Background="#21262D" CornerRadius="6" Padding="2" Margin="0,8,0,0">
                                        <controls:StackedBarChart x:Name="AnalyticsDailyBars" Height="120"/>
                                    </Border>
                                    <TextBlock x:Name="AnalyticsDailyEmptyText" Text="Daily activity will appear after your first day of use." Foreground="{StaticResource TextMuted}" FontSize="12" FontStyle="Italic" Margin="0,8,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Category Breakdown Card -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                <StackPanel>
                                    <TextBlock Text="By category this week" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <StackPanel x:Name="AnalyticsCategoryBreakdownPanel" Margin="0,12,0,0"/>
                                    <TextBlock x:Name="AnalyticsCategoryEmptyText" Text="Category breakdown will appear after this week's first accepted suggestion." Foreground="{StaticResource TextMuted}" FontSize="12" FontStyle="Italic" Margin="0,8,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Time-of-Day Card -->
                            <Border x:Name="AnalyticsTimeOfDayCard" Style="{StaticResource CardStyle}" Margin="0,0,0,18" Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock Text="When you write best" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <Grid x:Name="AnalyticsTimeOfDayGrid" Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Correction Trends Card -->
                            <Border x:Name="AnalyticsCorrectionCard" Style="{StaticResource CardStyle}" Margin="0,0,0,18" Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock Text="Correction frequency" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="AnalyticsCorrectionSubtitle" Text="How often you edit accepted completions" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,4,0,0"/>
                                    <Border Background="#21262D" CornerRadius="6" Padding="2" Margin="0,8,0,0">
                                        <controls:SparklineControl x:Name="AnalyticsCorrectionSparkline" Height="100"/>
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!-- Context Leaderboard Card -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                <StackPanel>
                                    <TextBlock Text="Strongest contexts" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <StackPanel x:Name="AnalyticsContextLeaderboardPanel" Margin="0,12,0,0"/>
                                    <TextBlock x:Name="AnalyticsContextEmptyText" Text="Context rankings will appear after Keystroke sees repeated patterns in specific threads or documents." Foreground="{StaticResource TextMuted}" FontSize="12" FontStyle="Italic" Margin="0,8,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Milestones Card -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                                <StackPanel>
                                    <TextBlock Text="Milestones" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <StackPanel x:Name="AnalyticsMilestonesPanel" Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                </StackPanel>

                <StackPanel x:Name="AppControlSection" Visibility="Collapsed">
                        <TextBlock Text="App Control" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="Decide exactly where Keystroke is welcome so you can keep it on without second-guessing the overlay." Style="{StaticResource SectionIntroStyle}"/>

                        <Border Style="{StaticResource CardStyle}" Margin="0,20,0,18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="280"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,18,0">
                                    <TextBlock Text="Per-app availability" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="SemiBold"/>
                                    <TextBlock Text="Use this when you want Keystroke everywhere except a few noisy apps, or only inside a short trusted list like Discord and Slack." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <ComboBox x:Name="AppFilteringModeCombo" Margin="0,16,0,0" SelectionChanged="AppFilteringModeCombo_Changed">
                                        <ComboBoxItem Tag="allow_all_except_blocked">All apps except blocked ones</ComboBoxItem>
                                        <ComboBoxItem Tag="allow_listed_only">Only listed apps</ComboBoxItem>
                                    </ComboBox>
                                    <TextBlock x:Name="AppFilteringDescriptionText" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Grid.Column="1" Style="{StaticResource SoftCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Quick tips" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock Text="Block games, remote desktops, terminals, or anything fullscreen if the overlay feels distracting." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                        <TextBlock Text="Use allow-list mode if you only want Keystroke in a few communication apps." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                        <TextBlock Text="Use “Add last” for the most recent app you were in, or the running-app picker when you want to choose explicitly." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource SoftCardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Common presets" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                <TextBlock x:Name="AppPresetSummaryText" Text="Start broad, then tighten Keystroke down only where you want it." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                <WrapPanel Margin="0,14,0,0">
                                    <Button Content="Everywhere except blocked" Style="{StaticResource PresetButton}" Margin="0,0,10,10" Click="AppPresetEverywhere_Click"/>
                                    <Button Content="Chat + email only" Style="{StaticResource PresetButton}" Margin="0,0,10,10" Click="AppPresetChatOnly_Click"/>
                                    <Button Content="Writing apps only" Style="{StaticResource PresetButton}" Margin="0,0,10,10" Click="AppPresetWritingOnly_Click"/>
                                    <Button Content="Manual allow list" Style="{StaticResource PresetButton}" Margin="0,0,10,10" Click="AppPresetManualAllowList_Click"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Style="{StaticResource SoftCardStyle}" Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="Last app you were using" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="LastExternalAppText" Text="No recent app captured yet." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap"/>
                                    <TextBlock x:Name="LastExternalAppMetaText" Text="" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    <TextBlock x:Name="LastExternalAppHintText" Text="Focus another app before opening Settings, or use the running-app picker on the right." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel Margin="0,14,0,0">
                                        <Button Content="Block last app" Style="{StaticResource SecondaryButton}" Click="AddLastAppToBlocked_Click" Margin="0,0,10,0"/>
                                        <Button Content="Allow last app" Style="{StaticResource SecondaryButton}" Click="AddLastAppToAllowed_Click"/>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource SoftCardStyle}" Margin="12,0,0,0">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Running app picker" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="SemiBold"/>
                                        <Button Grid.Column="1" Content="Refresh" Style="{StaticResource SecondaryButton}" Click="RefreshAppPicker_Click"/>
                                    </Grid>
                                    <TextBlock Text="Pick a visible app from the current desktop session when you want an explicit target." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <ComboBox x:Name="RunningAppsCombo" Margin="0,14,0,0" SelectionChanged="RunningAppsCombo_Changed"/>
                                    <TextBlock x:Name="RunningAppsSelectionDetailText" Text="Select an app to see more detail here." Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    <WrapPanel Margin="0,14,0,0">
                                        <Button Content="Block selected" Style="{StaticResource SecondaryButton}" Click="AddSelectedAppToBlocked_Click" Margin="0,0,10,0"/>
                                        <Button Content="Allow selected" Style="{StaticResource SecondaryButton}" Click="AddSelectedAppToAllowed_Click"/>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Grid Margin="0,0,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="BlockedAppsCard" Grid.Column="0" Style="{StaticResource InsetPanelStyle}" Margin="0,0,12,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Blocked apps" Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="SemiBold"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Add last" Style="{StaticResource SecondaryButton}" Click="AddCurrentBlockedApp_Click" Margin="0,0,8,0"/>
                                            <Button Content="Clear" Style="{StaticResource SecondaryButton}" Click="ClearBlockedApps_Click"/>
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock Text="One process name per line. Great for games, fullscreen apps, or anything where the overlay is distracting." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                    <TextBox x:Name="BlockedAppsBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="160" FontFamily="Consolas" FontSize="12" TextChanged="Setting_Changed"/>
                                </StackPanel>
                            </Border>

                            <Border x:Name="AllowedAppsCard" Grid.Column="1" Style="{StaticResource InsetPanelStyle}" Margin="12,0,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Allowed apps" Foreground="{StaticResource TextPrimary}" FontSize="14" FontWeight="SemiBold"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Add last" Style="{StaticResource SecondaryButton}" Click="AddCurrentAllowedApp_Click" Margin="0,0,8,0"/>
                                            <Button Content="Clear" Style="{StaticResource SecondaryButton}" Click="ClearAllowedApps_Click"/>
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock Text="Used when you want a tight allow list. Anything not listed here stays quiet." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                    <TextBox x:Name="AllowedAppsBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="160" FontFamily="Consolas" FontSize="12" TextChanged="Setting_Changed"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                </StackPanel>

                <StackPanel x:Name="AppearanceSection" Visibility="Collapsed">
                        <TextBlock Text="Appearance" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="Pick the panel theme and see how it changes the preview." Style="{StaticResource SectionIntroStyle}"/>

                        <Border Style="{StaticResource CardStyle}" Margin="0,20,0,18">
                            <StackPanel>
                                <TextBlock Text="Panel theme" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="These themes change the suggestion panel accent and glow without affecting your settings." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>

                                <UniformGrid Columns="5" Margin="0,18,0,0">
                                    <Button Tag="midnight" Click="Swatch_Click" Style="{StaticResource ThemeCardButtonStyle}" Margin="0,0,16,0">
                                        <Border Style="{StaticResource SoftCardStyle}" Padding="14">
                                            <StackPanel>
                                                <Grid Width="60" Height="60" HorizontalAlignment="Center">
                                                    <Ellipse x:Name="SwatchRingMidnight" Stroke="Transparent" StrokeThickness="3"/>
                                                    <Ellipse Fill="#5050E8" Width="42" Height="42"/>
                                                </Grid>
                                                <TextBlock Text="Midnight" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                    <Button Tag="ember" Click="Swatch_Click" Style="{StaticResource ThemeCardButtonStyle}" Margin="0,0,16,0">
                                        <Border Style="{StaticResource SoftCardStyle}" Padding="14">
                                            <StackPanel>
                                                <Grid Width="60" Height="60" HorizontalAlignment="Center">
                                                    <Ellipse x:Name="SwatchRingEmber" Stroke="Transparent" StrokeThickness="3"/>
                                                    <Ellipse Fill="#D06010" Width="42" Height="42"/>
                                                </Grid>
                                                <TextBlock Text="Ember" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                    <Button Tag="forest" Click="Swatch_Click" Style="{StaticResource ThemeCardButtonStyle}" Margin="0,0,16,0">
                                        <Border Style="{StaticResource SoftCardStyle}" Padding="14">
                                            <StackPanel>
                                                <Grid Width="60" Height="60" HorizontalAlignment="Center">
                                                    <Ellipse x:Name="SwatchRingForest" Stroke="Transparent" StrokeThickness="3"/>
                                                    <Ellipse Fill="#209A50" Width="42" Height="42"/>
                                                </Grid>
                                                <TextBlock Text="Forest" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                    <Button Tag="rose" Click="Swatch_Click" Style="{StaticResource ThemeCardButtonStyle}" Margin="0,0,16,0">
                                        <Border Style="{StaticResource SoftCardStyle}" Padding="14">
                                            <StackPanel>
                                                <Grid Width="60" Height="60" HorizontalAlignment="Center">
                                                    <Ellipse x:Name="SwatchRingRose" Stroke="Transparent" StrokeThickness="3"/>
                                                    <Ellipse Fill="#C83898" Width="42" Height="42"/>
                                                </Grid>
                                                <TextBlock Text="Rose" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                    <Button Tag="slate" Click="Swatch_Click" Style="{StaticResource ThemeCardButtonStyle}">
                                        <Border Style="{StaticResource SoftCardStyle}" Padding="14">
                                            <StackPanel>
                                                <Grid Width="60" Height="60" HorizontalAlignment="Center">
                                                    <Ellipse x:Name="SwatchRingSlate" Stroke="Transparent" StrokeThickness="3"/>
                                                    <Ellipse Fill="#586878" Width="42" Height="42"/>
                                                </Grid>
                                                <TextBlock Text="Slate" Foreground="{StaticResource TextSecondary}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </UniformGrid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Theme preview" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="See how the selected theme looks on a suggestion bubble." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>
                                <Border x:Name="AppearancePreviewBubble" Margin="0,14,0,0" Background="#101825" CornerRadius="{StaticResource RadiusCard}" Padding="16,12" BorderBrush="#4A5B7390" BorderThickness="2" HorizontalAlignment="Left" MaxWidth="460">
                                    <StackPanel>
                                        <TextBlock Text=", and I'll send the cleaned-up draft over later this afternoon." FontFamily="Cascadia Code, Consolas" FontSize="14" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                        <TextBlock Text="Tab accept &#x00b7; Ctrl+&#x2192; word &#x00b7; Esc dismiss" FontFamily="Segoe UI" FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                </StackPanel>

                <StackPanel x:Name="AdvancedSection" Visibility="Collapsed">
                        <TextBlock Text="Advanced" Style="{StaticResource SectionTitleStyle}"/>
                        <TextBlock Text="Provider setup, model choices, and prompt controls for advanced tuning. For most people, Gemini with Gemini 3.1 Flash-Lite Preview is the best default balance of speed, quality, and cost." Style="{StaticResource SectionIntroStyle}"/>

                        <Border Style="{StaticResource CardStyle}" Margin="0,20,0,18">
                            <StackPanel>
                                <TextBlock Text="Engine" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="Choose the provider that powers Keystroke, then fine-tune its details below. Gemini is the recommended default for most users." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>
                                <ComboBox x:Name="EngineCombo" Margin="0,16,0,0" SelectionChanged="EngineCombo_Changed">
                                    <ComboBoxItem Content="Gemini (recommended default)"/>
                                    <ComboBoxItem Content="GPT-5"/>
                                    <ComboBoxItem Content="Claude"/>
                                    <ComboBoxItem Content="OpenRouter (many models)"/>
                                </ComboBox>
                                <TextBlock x:Name="EngineOverrideNotice" Text="⚠ Overridden — a local model is active below" Foreground="{StaticResource Warning}" FontSize="11" Margin="0,8,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="License" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock x:Name="LicenseStatusText" Text="Free tier — 30 completions per day" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="LicenseKeyBox" Margin="0,0,8,0" FontFamily="Consolas" FontSize="13"/>
                                    <Button x:Name="ActivateLicenseButton" Grid.Column="1" Content="Activate" Style="{StaticResource PrimaryButton}" VerticalAlignment="Center" Click="ActivateLicense_Click"/>
                                </Grid>
                                <TextBlock x:Name="LicenseValidationText" Text="" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Provider details" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock x:Name="ProviderDetailIntroText" Text="Only the active provider is shown." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,6,0,0"/>

                                <StackPanel x:Name="GeminiApiKeyPanel" Margin="0,16,0,0">
                                    <TextBlock Text="Gemini API key" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="GeminiApiKeyBox" Margin="0,0,8,0" TextChanged="Setting_Changed"/>
                                        <TextBlock x:Name="GeminiApiKeyStatus" Grid.Column="1" Text="●" FontSize="16" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel x:Name="Gpt5ApiKeyPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="OpenAI API key" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="Gpt5ApiKeyBox" Margin="0,0,8,0" TextChanged="Setting_Changed"/>
                                        <TextBlock x:Name="Gpt5ApiKeyStatus" Grid.Column="1" Text="●" FontSize="16" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel x:Name="ClaudeApiKeyPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="Claude API key" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="ClaudeApiKeyBox" Margin="0,0,8,0" TextChanged="Setting_Changed"/>
                                        <TextBlock x:Name="ClaudeApiKeyStatus" Grid.Column="1" Text="●" FontSize="16" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel x:Name="GeminiModelPanel" Margin="0,16,0,0">
                                    <TextBlock Text="Gemini model" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="GeminiModelCombo" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="Gemini 3.1 Flash-Lite Preview (recommended default)" Tag="gemini-3.1-flash-lite-preview"/>
                                        <ComboBoxItem Content="Gemini 3 Flash Preview (balanced)" Tag="gemini-3-flash-preview"/>
                                        <ComboBoxItem Content="Gemini 3.1 Pro Preview (deepest)" Tag="gemini-3.1-pro-preview"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel x:Name="Gpt5ModelPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="GPT-5 model" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="Gpt5ModelCombo" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="GPT-5.4 nano (recommended)" Tag="gpt-5.4-nano"/>
                                        <ComboBoxItem Content="GPT-5.4 mini (balanced)" Tag="gpt-5.4-mini"/>
                                        <ComboBoxItem Content="GPT-5.4 (smartest)" Tag="gpt-5.4"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel x:Name="ClaudeModelPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="Claude model" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="ClaudeModelCombo" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="Claude Haiku 4.5 (recommended)" Tag="claude-haiku-4-5"/>
                                        <ComboBoxItem Content="Claude Sonnet 4.6 (smartest)" Tag="claude-sonnet-4-6"/>
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel x:Name="OpenRouterPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="OpenRouter API key" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="OpenRouterApiKeyBox" Margin="0,0,8,0" TextChanged="Setting_Changed"/>
                                        <TextBlock x:Name="OpenRouterApiKeyStatus" Grid.Column="1" Text="●" FontSize="16" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
                                    </Grid>
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="OpenRouter model" Foreground="{StaticResource TextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                                        <Button x:Name="OpenRouterRefreshBtn" Grid.Column="1" Content="Refresh" Style="{StaticResource SecondaryButton}" Click="OpenRouterRefresh_Click"/>
                                    </Grid>
                                    <ComboBox x:Name="OpenRouterModelCombo" SelectionChanged="OpenRouterModelCombo_SelectionChanged"/>
                                    <TextBlock x:Name="OpenRouterModelStatus" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,8,0,0"/>
                                    <TextBlock x:Name="OpenRouterReasoningWarning" Text="Reasoning-first models tend to be slower and less consistent for autocomplete." Foreground="{StaticResource Warning}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="System prompt" Foreground="{StaticResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
                                    <Button Grid.Column="1" Content="Reset to default" Style="{StaticResource SecondaryButton}" Click="ResetPrompt_Click"/>
                                </Grid>
                                <TextBlock Text="These instructions are sent to the model. Only edit them if you want to deeply customize behavior." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,14" TextWrapping="Wrap"/>
                                <TextBox x:Name="PromptBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="320" FontFamily="Consolas" FontSize="12" TextChanged="Setting_Changed"/>
                            </StackPanel>
                        </Border>

                        <!-- ── Local Models (Experimental) ── -->
                        <Border Style="{StaticResource ExperimentalCardStyle}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Local Models — Experimental" Foreground="{StaticResource Warning}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Text="Run a local model via Ollama instead of a cloud provider. Quality varies by model and hardware — the cloud engine above is ignored while this is on." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <CheckBox x:Name="UseLocalModelCheck" Grid.Column="1" Style="{StaticResource SwitchCheckBoxStyle}" Checked="UseLocalModelCheck_Changed" Unchecked="UseLocalModelCheck_Changed" VerticalAlignment="Center"/>
                                </Grid>

                                <StackPanel x:Name="LocalModelConfigPanel" Visibility="Collapsed" Margin="0,16,0,0">
                                    <TextBlock Text="Ollama model" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,0,0,6"/>
                                    <ComboBox x:Name="OllamaModelCombo" SelectionChanged="Setting_Changed">
                                        <ComboBoxItem Content="Qwen 3 30B A3B (best local quality)" Tag="qwen3:30b-a3b"/>
                                        <ComboBoxItem Content="Qwen 3 8B (recommended local default)" Tag="qwen3:8b"/>
                                        <ComboBoxItem Content="Mistral 7B (clean prose)" Tag="mistral:7b"/>
                                        <ComboBoxItem Content="Llama 3.1 8B (solid fallback)" Tag="llama3.1:8b"/>
                                    </ComboBox>
                                    <TextBlock Text="Endpoint" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,14,0,6"/>
                                    <TextBox x:Name="OllamaEndpointBox" TextChanged="Setting_Changed"/>
                                    <TextBlock x:Name="OllamaStatus" Text="Checking..." Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,10,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                </StackPanel>
                </Grid>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="1" Background="{StaticResource BgSidebar}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="4" Padding="18,14" Margin="0,18,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Auto-save is on" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="Every change updates immediately." Foreground="{StaticResource TextMuted}" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SaveStatus" Text=" Saved" Foreground="{StaticResource Success}" FontSize="12" FontWeight="SemiBold" Opacity="0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Done" Style="{StaticResource PrimaryButton}" Click="Done_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
